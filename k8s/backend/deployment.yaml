apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlbackend
  labels:
    app: mlbackend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mlbackend
  template:
    metadata:
      labels:
        app: mlbackend
    spec:
      containers:
      - name: mlbackend
        image: dtechafrica/mlacademyhub:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: mlbackend-config
        - secretRef:
            name: mlbackend-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
